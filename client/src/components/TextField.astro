---
interface Props {
  label?: string;
  type?: any;
  name: string;
  placeholder?: string;
  required?: boolean;
  value?: string;
  class?: string;
  labelClass?: string;
  [key: string]: any;
}

const {
  label,
  type = 'text',
  name,
  placeholder,
  required = false,
  value,
  class: className = '',
  labelClass = 'mb-1.5 block text-xs font-normal text-text-black',
  ...rest
} = Astro.props as Props;

const inputClass =
  `w-full rounded-lg border border-line-200 bg-white px-3.5 py-2.5 text-sm text-gray-700 placeholder:text-text-gray-50 focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/30 ${className}`;
---

{label ? (
  <label class="block">
    <span class={labelClass}>
      {label}
    </span>

    <input
      type={type}
      name={name}
      id={name}
      placeholder={placeholder}
      required={required}
      value={value}
      class={inputClass}
      {...rest}
    />
  </label>
) : (
  <input
    type={type}
    name={name}
    id={name}
    placeholder={placeholder}
    required={required}
    value={value}
    class={inputClass}
    {...rest}
  />
)}